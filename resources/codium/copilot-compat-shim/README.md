# GitHub Copilot Compatibility Shim

This local-only extension satisfies VSCodium's baked-in `GitHub.copilot` default chat-agent extension slot while the chat implementation is provided by `GitHub.copilot-chat`.
